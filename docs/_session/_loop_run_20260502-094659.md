# Loop Run 2026-05-02 09:46:59

## Task
- ID: N-001b（LINE 客服 AI 撤掉）
- 描述: 根據 William 2026-05-02 拍板「Line 客服 AI 的售價、行程名、供應商分類等功能先撤掉；在完全移除 Line 客服功能後、我們會重新撰寫」、整套 AI 客服撤掉、改 fallback 訊息

## 改動

### 刪除
- `src/lib/line/ai-customer-service.ts` 整檔（507 行 AI 對話邏輯、含三個 schema bug 欄位查詢）

### 修改
- `src/app/api/line/webhook/route.ts`：
  - 拿掉 `handleAICustomerService` import
  - 加常數 `AI_FALLBACK_REPLY = '感謝您的訊息！我們已收到、將盡快回覆您。'`
  - `handleAIMessage` 改：不 call AI、用 fallback 訊息回覆、仍存對話到 DB（給人工客服查歷史）
- `src/app/api/meta/webhook/route.ts`（Facebook / Instagram）：
  - 拿掉 `handleAICustomerService` import
  - 加同樣的 `AI_FALLBACK_REPLY` 常數
  - try/catch block 簡化成直接 sendReply fallback、不再分 AI 成功 / 失敗

### 保留（之後重寫 AI 時還能用）
- 對話記錄到 DB（`saveConversationToDb` / `ai_conversations` 表）
- 人工轉接邏輯（`humanHandoverUsers`、`HANDOVER_KEYWORDS`、`MAX_AI_ROUNDS`）
- LINE / Meta webhook 簽名驗證、rate limit、idempotency

## 解決的 schema bug

N-001 暴露的 3 個 schema bug 一次清掉（隨 file 一起砍）：
- `tours.selling_prices` 不存在
- `itineraries.name` 不存在
- `suppliers.category` 不存在

## 驗證
- `tsc --noEmit` → 0 errors
- `check-standards.sh --strict` → 11/11 全綠

## 結果
✅ 留下、ready for human review

- Branch: `auto/N-001b-disable-line-ai-customer-service`
- 從 main 切

## N-001 暴露的 6 個 bug 處理進度（更新）

| Bug | 狀態 |
|---|---|
| ✅ tours.channel_id (channel-notify.ts) | 整套砍（N-001b-fix-schema-bugs） |
| ✅ tours.selling_prices / itineraries.name / suppliers.category (ai-customer-service.ts) | 整檔砍（本 branch） |
| ✅ payment_request_items.amount (vouchers/auto-create) | 改 subtotal（N-001b-fix-schema-bugs） |
| ⏳ member_type missing (orders/create-from-booking) | 等 William 從 4 選項拍板 |
| ✅ customers/link-line type | 整檔砍（N-001b-fix-schema-bugs） |
| ✅ contract_data jsonb (public/contract/sign) | 修對（N-001b-fix-schema-bugs） |

**剩 1 個（member_type 等拍板）**。

## 副作用 / 下次注意

- AI 客服 GEMINI_API_KEY 環境變數現在沒人用、但保留無傷（之後重寫會用回來）
- `saveConversationToDb` 仍走 LINE webhook 內部 raw fetch + service_role apikey header pattern（屬 N-001c 範圍、本次未動）
