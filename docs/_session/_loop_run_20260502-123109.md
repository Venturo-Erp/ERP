# Loop Run 2026-05-02 12:31:09

## Task
- ID: T7 final（capability 全清重來）
- 描述: William 拍板「業務員全員請調、自己動手做」、整體 reset role_capabilities、跑乾淨重來

## William 拍板（2026-05-02）

> 「這些業務員全員請調、沒現場的東西、也沒需要用單、所以反正現在就是自己動手做。把這件事寫好。」

= 採納「全清重來」方向、不擔心既有非 admin role 的權限設定被砍。

## 改動

### Migration: `20260503100000_reset_role_capabilities.sql`
```sql
DELETE FROM public.role_capabilities
WHERE role_id IN (
  SELECT r.id FROM public.workspace_roles r
  JOIN public.workspaces w ON w.id = r.workspace_id
  WHERE w.code <> 'VENTURO'
);
```

清掉所有非 VENTURO 的 capability mapping、保留漫途平台主管（N-M02 設定）。

### 重新跑 sync
- 4 個租戶系統主管 role 各自掛上 106 個 MODULES 衍生的乾淨 capability
- 非 admin role（業務 / 會計 / 助理 / 團控 / 導遊 / 領隊）變空 = 0 caps

## 最終狀態

```
workspace    role                 caps
----------------------------------------
✅ CORNER     系統主管               106    ← MODULES 全套
⏸ CORNER     助理                 0       ← 待 William 指派
⏸ CORNER     團控                 0
⏸ CORNER     導遊                 0
⏸ CORNER     會計                 0
⏸ CORNER     業務                 0
⏸ CORNER     領隊                 0
✅ JINGYAO    系統主管               106
⏸ JINGYAO    各 non-admin role    0       ← 同樣待指派
✅ TESTUX     系統主管               106
✅ VENTURO    平台主管               3      ← N-M02 設定保留
✅ YUFEN      系統主管               106
```

死碼全消：channel.* / dashboard.* / wishlist.* / design.* / 舊 tenants.* / 舊 customers.* 已不存在。

## 給 William 的下一步操作

### 1. 重新指派 non-admin role 權限

在 ERP 內 → HR → 職務管理頁面、給每個 non-admin role 勾選 capability：

**典型「業務」role 建議勾選**：
- `tours.read` / `tours.orders.read` / `tours.orders.write` / `tours.members.read`
- `orders.*`（全套）
- `database.customers.read` / `database.customers.write`
- `finance.payments.read`（看收款進度）

**典型「會計」role 建議勾選**：
- `finance.*`（全套）
- `accounting.*`（全套）
- `tours.read`（看團）

**典型「助理」role 建議勾選**：
- `tours.read` / `orders.read` / `database.customers.read`

但這些是業務判斷、由你拍板實際勾選。

### 2. 之後的開發者要加新功能

照 `docs/MODULE_REGISTRY.md` 流程：
1. 改 `src/lib/permissions/module-tabs.ts` MODULES 加新 tab
2. 跑 `npx tsx scripts/sync-capabilities.ts`
3. 系統主管 role 自動掛上新 capability、其他 role 你決定要不要勾

### 3. 之後我可以幫你做

如果你要、我可以寫 default role 模板 script、一鍵把 4 個租戶的「業務」/「會計」/「助理」role 套上預設權限（你拍板模板內容、我寫）。

## 驗證
- `tsc --noEmit` → 0 errors
- `check-standards.sh --strict` → 11/11 全綠
- DB 抽查：死碼全消、admin role 各 106 caps、non-admin 0 caps、VENTURO 平台主管 3 caps 保留

## 結果
✅ commit 進 main、autoresearch 第二波結束

## autoresearch 全部進度（main 上）

| Task | 狀態 |
|---|---|
| T1 N-003 驗證 | ✅ |
| T2 update_updated_at_column 全清 | ✅ |
| T3 line/webhook raw fetch 收斂 | ✅ |
| T5 API 守門 audit | ✅ |
| T6 紅燈 endpoint + 相關刪除 | ✅ |
| T7 Step A-D capability sync 系統 | ✅ |
| T7 Step E.1 endpoint mapping dry-run | ✅（未 apply）|
| T7 Step E.2 RLS rewrite dry-run | ✅（標記「不要 apply、判斷錯誤」）|
| T7 final capability 全清重來 | ✅（本次）|

## 未完成（之後做）

- 業務 / 會計 / 助理 role 重新指派權限（William 自己做）
- 讀取量優化（William 重點 #2、整體 ERP 列表頁改 20 筆）
- 平台級 endpoint 加守門（機器人 / AI 設定頁、未來重寫時做）
