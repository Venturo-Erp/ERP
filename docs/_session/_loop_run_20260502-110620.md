# Loop Run 2026-05-02 11:06:20

## Task
- ID: T5（API 守門 audit、純 read、產 report）
- 描述: 對 91 個 API route 跑自動化掃描、列出守門狀況、找出紅燈

## 方法

對 `src/app/api/**/route.ts` 用 grep 統計：
- `auth` = `getServerAuth` / `getApiContext` / `withAuth` 出現次數
- `cap` = `hasCapabilityByCode` / `capabilityCode` / `CAPABILITIES.` 出現次數
- `admin` = `getSupabaseAdminClient` / `service_role` 出現次數
- `ws_filter` = `auth.data.workspaceId` / `workspace_id` 出現次數
- `public` = 是否在 middleware PUBLIC_PATHS 清單

完整 raw data 在 `docs/_session/_api_audit_raw_20260502.txt`。

## 守門分布（91 個 route）

| 類別 | 數量 | 說明 |
|---|---|---|
| 完全無守門（auth=0、cap=0）| 49 | 含 public endpoint + middleware-handled |
| 有 auth 但無 capability check | 37 | 「登入就能用」、未細分權限 |
| 有 capability check | **5** | ⚠️ 只有 5 個 endpoint 真的細分權限 |

> 5/91 = 5.5% 真細分權限。其他 86 個要嘛是「登入就過」、要嘛靠其他機制（middleware / RLS）。

## 🔴 紅燈清單（用 admin client 繞 RLS + 無守門 + 非 public）

這 5 個 endpoint **直接用 service_role 客戶端、沒寫明 public、且沒任何 auth check**：

| Route | admin 用次數 | ws_filter | 風險判斷 |
|---|---|---|---|
| `/api/bot-notification` | 2 | 7 | 看起來是內部 bot 觸發、應該加 internal secret |
| `/api/bot/ticket-status` | 4 | 18 | 同上 |
| `/api/line/send-insurance` | 2 | 1 | 之前 N-001b 看過、內部 user 觸發、應加 auth |
| `/api/tours/by-code/[code]` | 2 | 1 | 設計上是公開（之前 N-001 註解寫過）、應該加進 PUBLIC_PATHS |
| `/api/transport/send-booking` | 2 | 0 | 內部 user 觸發、無 ws filter、**真風險** |

## 🟡 黃燈清單（無守門 + 無 admin + 非 public、可能是 middleware 自動 catch）

部分樣本：
- `/api/contracts/list` `/api/contracts/create` `/api/contracts/members` `/api/contracts/[id]/pdf` `/api/contracts/paper-sign`
- `/api/accounting/period-closing` `/api/accounting/vouchers/create`
- `/api/finance/accounting-subjects` `/api/finance/expense-categories` `/api/finance/payment-methods`
- `/api/bank-accounts`
- `/api/roles` `/api/roles/[roleId]`
- `/api/orders/create-from-booking`

這些要實際看 file 才能確認：
1. middleware 已自動處理（用 cookie session 拿 user_id、再用 RLS 過濾）
2. 或真的沒守門

我的 grep 太粗、可能誤判。需要逐個 review。

## ⚠️ 5 個有 capability check 的（基線）

這 5 個是現在系統「真正細分權限」的代表：
- `/api/permissions/features` — 平台級
- `/api/workspaces/[id]` — 平台級
- `/api/workspaces` — 平台級
- `/api/d/[code]` — 公開短網址
- `/api/tenants/create` — 平台級

注意：5 個之中 4 個是平台級（`platform.tenants.*`）、業務級 endpoint **沒有任何**用 capability check。

## 對 William 的建議

### 立即可做（你拍板我執行）

**Action 1：紅燈 5 個 endpoint 加守門**
每個都需要你拍板「該加什麼層級」：
- `/api/bot-notification` → internal secret 還是 platform.bots.write？
- `/api/bot/ticket-status` → 同上
- `/api/line/send-insurance` → tours.write 還是 platform.bots.write？
- `/api/tours/by-code/[code]` → 加進 PUBLIC_PATHS 即可
- `/api/transport/send-booking` → tours.write？

**Action 2：把「登入就過」的 37 個 endpoint 細分權限**
這個範圍大、應該按模組逐批做（顧客 module 一批、財務 module 一批等）。每批要你拍板「這個 module 的 endpoint 該掛什麼 capability」。

### 不該做（規避）

- 我不主動加守門 — 業務語意需要你拍板
- 不擴大範圍把 91 個全改 — 用「按模組分批」做、有 rollback 餘地

## 結果

✅ Audit report 留下、ready for human review

- Branch: main（直接 commit、沒切 branch、純 read 操作）
- 改動：純 docs（audit raw + 本 report）

## autoresearch 進度

- ✅ T1 N-003 驗證
- ✅ T2 update_updated_at_column 全清
- ✅ T3 line/webhook raw fetch 收斂
- ✅ T5 API 守門 audit（純 audit、產 report）
- ⏸️ T4（加守門）— 等你拍板紅燈 5 個的層級
- ⏸️ T6（RLS audit）— 範圍類似 T5、可下次做

## 我建議現在停下、等你看

T4-T6 都需要業務拍板（每個 endpoint 該掛什麼 capability）、我繼續做會做不對。

你看完紅燈清單後決定：
1. 紅燈 5 個馬上修（你說該掛什麼、我加）
2. 或先擱置、優先做別的（讀取量優化等）
