# Loop Run 2026-05-02 11:03:18

## Task
- ID: T3（N-001c：line/webhook/route.ts raw fetch 改用 admin client SSOT）
- 描述: 把 LINE webhook 內 ~12 處 raw fetch（直接打 Supabase REST API + service_role key 當 apikey header）改用 `getSupabaseAdminClient()` 統一 SSOT

## 改動

### 新增 helper
- `getLineWorkspaceId()` — 抽出 workspace_line_config 查詢、避免 6 處重複 boilerplate

### 改寫的 raw fetch（全部用 supabase client）
1. `saveUserToDb`：line_users upsert
2. `markUserUnfollowed`：line_users update
3. `saveGroupToDb`：line_groups upsert（含 workspace_line_config 查詢併進 helper）
4. `processCustomerBinding`：customers select + update
5. `processEmployeeBinding`：employees select + update
6. `saveConversationToDb`：customer_service_conversations insert
7. `processClockIn`：line_users select（找已綁定員工）

### 保留（不該改的）
- `x-internal-secret` header 用 `process.env.SUPABASE_SERVICE_ROLE_KEY!`（是 internal API 共享 secret、不是 createClient pattern）
- LINE Messaging API 的 fetch（`api.line.me/v2/bot/...`、外部第三方 API）
- 內部 Next.js API call（`baseUrl/api/...`）

## 驗證
- typed admin client 沒撞到 schema bug（line_users / line_groups / customers / employees / customer_service_conversations 全部 type 對齊）
- `tsc --noEmit` → 0 errors
- `check-standards.sh --strict` → 11/11 全綠
- `npm run build` → 18.1s 成功

## 結果
✅ N-001 系列「admin client SSOT 收斂」收尾。

整個 src/ 範圍：
- 之前散落 22 處（N-001 修了 16 處、N-001b 修 5 處）
- 現在剩 1 處：`x-internal-secret` 的 service_role 當 internal secret 用（合法、非 createClient）
- 唯一 admin client SSOT：`getSupabaseAdminClient()`

## autoresearch 進度
- ✅ T1 N-003（F5 已完、純驗證）
- ✅ T2 update_updated_at_column 全清（trigger SSOT 收斂三部曲完成）
- ✅ T3 line/webhook raw fetch 收斂（admin client SSOT 收尾）

下一個：T4（平台級守門 — 機器人 / AI 設定加 platform.bots / platform.ai capability）
