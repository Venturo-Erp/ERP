# Loop Run 2026-05-02 10:16:32

## Task
- ID: N-001b（訂單成員年齡自動分類）
- 描述: 根據 William 2026-05-02 拍板「用年紀區分成員類型、自動算」、5 級分類

## William 拍板細節

### 年齡分級（5 級）
| 類型 | 年齡 |
|---|---|
| infant（嬰兒）| 未滿 2 歲 |
| child_a（兒童 A）| 2-5 歲 |
| child_b（兒童 B）| 6-11 歲 |
| adult（成人）| 12-64 歲 |
| senior（長者）| 65 歲以上 |

### age 欄位處理
- **保留**（方便觀看）
- 但**系統自動算、application 不准手填**
- birth_date 是 SSOT、age + member_type 是衍生

### 訂單列表組成統計
- William 拍板：**不用做**

## 改動

### Migration
1. `supabase/migrations/20260503050000_member_type_auto_classify.sql`
   - DROP 舊 check constraint（原本只允許 adult/child/infant、3 級）
   - ADD 新 check constraint（5 級）
   - CREATE FUNCTION `compute_member_age_and_type()` — 從 birth_date 自動算 age + member_type
   - CREATE TRIGGER `compute_order_members_age_and_type` ON order_members BEFORE INSERT/UPDATE
   - ALTER COLUMN member_type SET DEFAULT 'adult'（沒生日 fallback）
   - Backfill 既有 117 筆有 birth_date 的（暫時 disable `tg_lock_order_members_ongoing`、跑完 enable）
   - pg_cron 每天 00:00 UTC 自動重算（生日過了會自動 +1）

2. `supabase/migrations/20260503060000_member_age_negative_guard.sql`（patch）
   - 上一個 migration backfill 後發現 2 筆生日是未來日期（朱知遠 2047-07-27、鄭勝燦 2050-11-07、明顯民國年寫成西元）
   - 負年齡 -21、-24 被分類成 infant、荒謬
   - 修 trigger function：負年齡 fallback `adult`、不歸 infant
   - 重 backfill 那 2 筆
   - cron 排程同樣加防呆

### Src
無（純 DB 改動、application 不需配合）

## 驗證

### Backfill 結果
| 類型 | 人數 | 年齡範圍 |
|---|---|---|
| adult | 132 | 13-64（其中 2 筆是負年齡的 fallback）|
| senior | 10 | 65-74 |
| child_b | 5 | 6-11 |
| child_a | 3 | 2-4 |
| infant | 0 | -（這批團員沒有 0-1 歲嬰兒）|

加上 33 筆沒生日的（仍是 adult）= 共 150 筆全分類完。

### 守門
- `tsc --noEmit` → 0 errors
- `check-standards.sh --strict` → 11/11 全綠

### Cron 已掛
```
jobname: recompute-order-members-age-daily
schedule: 0 0 * * *  (每天 00:00 UTC)
```

## 結果
✅ 留下、ready for human review

- Branch: `auto/N-001b-member-type-auto-classify`
- 從 main 切、純 2 個 migration、無 src 改動

## ⚠️ 給 William 的提醒（資料品質問題、不在本 task 範圍）

backfill 過程發現 2 筆團員生日寫成未來日期（明顯是民國年寫成西元）：

| 姓名 | 現況 birth_date | 看起來應該是 |
|---|---|---|
| 朱知遠 | 2047-07-27 | 1947-07-27（民國 36 年）？|
| 鄭勝燦 | 2050-11-07 | 1950-11-07（民國 39 年）？|

我**沒自動修**這 2 筆 — 這是業務資料、需要你或客服確認真實生日。它們的 member_type 已 fallback 到 adult、不影響邏輯。

要修的話、隨時告訴我、我寫個小 migration 改掉。

## 副作用 / 提醒

- Application code（`src/`）不該再對 order_members.age 直接賦值 — trigger 會蓋掉
- 之後 form / dialog 寫進 birth_date 後、自動會有 age + member_type
- 旅行業未來規範可從 member_type 衍生：機票價格、保險等級、飯店佔床 etc.
