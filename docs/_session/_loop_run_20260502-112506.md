# Loop Run 2026-05-02 11:25:06

## Task
- ID: T6（紅燈 endpoint + 相關概念整套刪除）
- 描述: William 2026-05-02 拍板「這些都可以刪除、先重寫」、把 T5 audit 標出的紅燈 5 個 + 相關 LINE 發送系列 + 公開頁面整套砍乾淨

## William 拍板細節

T5 列出 5 個紅燈 endpoint、William 拍板 → **全砍**：
- bot-notification / bot/ticket-status / line/send-insurance / tours/by-code / transport/send-booking

加上「跟它相關的概念都可以刪除掉」、所以連帶清光：
- LINE 發送系列（4 個 send-* + insurance-auto-save）
- cron/ticket-status（cron job 跟 bot/ticket-status 配套）
- 公開頁（insurance / booking / itinerary / transport/confirm）
- transport [id]/confirm API（配套車行頁）

## 改動

### 整個目錄刪除（11 處）

**API**：
- `src/app/api/bot-notification/`
- `src/app/api/bot/`（含 ticket-status）
- `src/app/api/cron/ticket-status/`
- `src/app/api/line/send-insurance/`
- `src/app/api/line/send-requirement/`
- `src/app/api/line/send-transport-quote/`
- `src/app/api/line/send-transport/`
- `src/app/api/tours/by-code/`
- `src/app/api/transport/`（含 send-booking + [id]/confirm）

**Public 頁面**：
- `src/app/public/insurance/`
- `src/app/public/booking/`
- `src/app/public/itinerary/`
- `src/app/transport/`（[id]/confirm 公開頁、給車行填）

### 修改

- `src/features/tours/components/TourActionButtons.tsx`：
  - 拿掉 `handleCopyLink` 函式（複製公開行程連結、link 到已砍的 /public/itinerary）
  - 拿掉「分享」按鈕（用 Share2 icon、call handleCopyLink）
  - 移除 `Share2` icon import

### 保留（共用 helper、別處還在用）
- `src/lib/cron/heartbeat.ts` — `process-tasks` cron 還在用
- LINE / Meta webhook（接收外部訊息）— 之前 N-001b 改成 fallback 訊息、保留接收能力
- `/public/contract/sign/` — 合約簽署、N-001b 修過、保留
- `src/features/confirmations/components/TransportQuoteDialog.tsx.backup` — backup file、不在我範圍

## 驗證
- 清 `.next` 快取後：`tsc --noEmit` → 0 errors
- `check-standards.sh --strict` → 11/11 全綠
- `npm run build` → 20.5s 成功

## 結果
✅ 全砍乾淨、commit 進 main

## 副作用 / 業務影響

**砍掉的功能**：
1. **機器人自動通知** — bot-notification + ticket-status cron。原本在做什麼不清楚（看 code 是查機票狀態 + 發 LINE 通知）、整套停用。
2. **業務員 trigger 發 LINE 通知供應商** — 保險團員名單 / 需求單 / 車行報價 / 車行委託 4 個流程、原本業務員按按鈕會發 LINE 訊息給供應商。整套停用、要重寫。
3. **公開預訂 + 公開行程展示 + 公開保險頁** — 客戶分享連結看 tour、訂位、保險團員名單頁。整套停用。
4. **車行委託確認流程** — 車行收 LINE 後填司機資訊的公開頁。整套停用。
5. **「複製公開行程連結」按鈕** — TourActionButtons 的 Share2 按鈕。

**之後重寫時注意**（William）：
- 重寫前先想清楚每個流程的「是平台還是租戶」、加正確 capability 守門
- 重寫公開頁時、把公開 path 加進 middleware PUBLIC_PATHS（避免再變紅燈）
- 重寫機器人通知時、考慮用 internal secret + platform.bots.* capability 雙保險

## autoresearch 進度

- ✅ T1 N-003 驗證
- ✅ T2 update_updated_at_column 全清
- ✅ T3 line/webhook raw fetch 收斂
- ✅ T5 API 守門 audit
- ✅ T6 紅燈 endpoint + 相關概念整套砍除
- ⏸️ T4 加守門 — 紅燈砍掉了、剩下「黃燈」37 個 endpoint 的細分權限、需 William 業務拍板
- ⏸️ RLS audit — 純 read、可下次做
- ⏸️ 讀取量優化 — William 重點 #2、需業務拍板每個列表頁的篩選欄位

## 下一輪建議

紅燈處理完、剩下需要 William 拍板的 task：
1. 黃燈 37 個 endpoint 細分權限（按 module 分批）
2. 讀取量優化（按 module 分批）
3. RLS audit（純 read、agent 可做）

我建議先做 RLS audit（純 read、跟 T5 同模式）、產 report 給 William 看。
