# Loop Run 2026-05-02 10:48:51

## Task
- ID: N-M02（平台 / 租戶分離）
- 描述: 建漫途整合行銷 platform workspace、純靠 capability 驅動分離平台主 vs 租戶員工

## William 拍板細節

- 漫途 = 一個普通 workspace（不加特殊邏輯區分）
- 平台 vs 租戶純靠 **既有 capability 系統判斷**（既有 sidebar 不變、自動正確）
- 漫途 code = `VENTURO`、name = `漫途整合行銷`
- William 在漫途用 `E001`（同 user_id、不同 employee record）
- Capability 命名前綴：`platform.*` = 平台級、其他 = 租戶級

## 改動

### Migration: `20260503070000_create_venturo_platform_workspace.sql`

1. **順手清死 trigger**：`create_default_finance_settings` 之前 INSERT 進已 DROP 的 `accounting_subjects` 表、會炸新建 workspace。改寫拿掉死段、保留 payment_methods + bank_accounts 預設邏輯。
2. **建漫途 workspace**：code='VENTURO'、name='漫途整合行銷'、type='platform'
3. **建漫途的「平台主管」role**：is_admin=true
4. **William 在漫途加 employee record**：employee_number='E001'、user_id 同 Corner（同帳號、不同身份）
5. **Capability rename**：
   - `settings.tenants.read` → `platform.tenants.read`
   - `settings.tenants.write` → `platform.tenants.write`
6. **把 platform.* capability 從非漫途 role 全部移除**：原本 Corner / 御風 等 admin role 都有 `platform.is_admin` + `settings.tenants.*`、現在全部清除
7. **給漫途「平台主管」掛全套 platform capability**：3 個都掛上

### Src 修改（capability code reference）

- `src/app/api/workspaces/[id]/route.ts`：2 處 `'settings.tenants.write'` → `'platform.tenants.write'`
- `src/app/api/permissions/features/route.ts`：1 處 capability code + 註解
- `src/lib/auth/get-api-context.ts`：JSDoc example 更新

## 驗證

### DB 抽查
```
VENTURO workspace + 平台主管 role + William E001 ✓
platform.is_admin / platform.tenants.read / platform.tenants.write
  → 全部只掛在 VENTURO 的「平台主管」角色（其他 workspace 0 個）
```

### 守門
- `tsc --noEmit` → 0 errors
- `check-standards.sh --strict` → 11/11 全綠
- `npm run build` → 24.7s 成功

## 結果
✅ 留下、ready for human review

- Branch: `auto/N-M02-platform-tenant-split`
- 從 main 切（含先前 11 個 task 的 commit）

## 行為改變（William 用得到）

### 你登入 Corner
- 還是 Corner 員工（E001、admin role）
- **Sidebar 自動拿掉**「租戶管理」menu（因為 Corner role 沒 `platform.tenants.read` 了）
- 業務 ERP 全部正常用

### 你登入漫途整合行銷（公司代號 VENTURO）
- 是漫途的「平台主管」（E001、is_admin role）
- **Sidebar 只看到平台級**頁面：租戶管理 / 機器人設定 / API 設定
- 業務 ERP 全部看不到（沒掛業務 capability）

### 其他人（御風 / Carson / Jess 等）
- 完全不受影響、繼續看到自己的業務 ERP
- 之前如果有不該有的 `platform.is_admin`、現在被清掉、變回正常租戶員工

## 副作用提醒

- **Calendar 全租戶模式**：之前用 `platform.is_admin` 判斷「能看跨租戶資料」、現在 William 登入 Corner 已經沒這 capability。要看跨租戶 → 登入漫途
- **某些 admin-only 介面**（如 settings 管理）登入 Corner 還能看（因為 role.is_admin=true），但「平台級判斷」會失效。這是預期的、不算 bug
- **未來機器人 / AI 設定頁**要加 `platform.bots.*` / `platform.ai.*` capability 守門 → 之後另案做（不在本 task 範圍）

## 沒做的事（之後）

- LINE / Meta webhook 設定頁加 `platform.bots.*` 守門
- AI 平台設定頁加 `platform.ai.*` 守門
- 全域景點 / 國家 / 城市資料庫的訂閱訪問機制（W 講「未來營利」）
- 漫途首頁 / dashboard（目前登入漫途看到的是預設首頁）
