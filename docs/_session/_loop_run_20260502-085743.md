# Loop Run 2026-05-02 08:57:43

## Task
- ID: B-003 + B-004（合併、純 docs 改、不分開沒意義）
- 描述: 把 `line_messages` / `quote_confirmation_logs` 兩張 append-only log 表標例外、憲法補豁免條款

## 改動

### `docs/VENTURO_ERP_STANDARDS.md` Section 2「例外」
新增 append-only log 豁免條款：
> **Append-only log 表**（純記事件、寫入後不再 UPDATE、如 `line_messages` / `quote_confirmation_logs`）可豁免 `updated_at` 欄位跟對應 trigger。理由：log 表只新增不更新、`updated_at` 永遠等於 `created_at`、徒增一個欄位 + 一個 trigger 而沒用途。例外條目同樣要在 `SCHEMA_PLAN.md` 註明。

### `docs/SCHEMA_PLAN.md`
兩張表用途欄補例外標註：
- `quote_confirmation_logs`：「**例外**：append-only log、無 `updated_at`（憲法 §2）」
- `line_messages`：同上

## 驗證
- `tsc --noEmit` → 0 errors
- `check-standards.sh --strict` → 11/11 全綠
- DB 抽查：兩張表只有 `created_at`、無 `updated_at`（透過 information_schema.columns 確認）

## 結果
✅ 留下、ready for human review

- Branch: `auto/B-003-mark-log-table-exceptions`（從 main 切、不依賴 B-001/B-002）
- 改 docs 兩份、無 migration、無 src 改

## 異常與發現

無。純 docs 規範補洞、無業務影響。
