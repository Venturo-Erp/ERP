# Loop Run 2026-05-02 09:58:44

## Task
- ID: N-M04（內部聊天模組整套刪除）
- 描述: 根據 William 2026-05-02 拍板「直接完全刪除、不需要區分解凍或凍結、直接移除即可」、憲法 §16 凍結模組徹底清光

## 改動清單

### Migration（DB）
- `supabase/migrations/20260503040000_drop_channel_chat_system.sql`：DROP 4 張表 CASCADE
  - `channels` (22 cols)
  - `messages` (19 cols)
  - `channel_members` (8 cols)
  - `channel_groups` (9 cols)
- DB 已套用、確認 4 張表都不存在

### Src 整檔/整目錄刪除

**UI 模組**：
- `src/app/(main)/channel/` 整目錄
- `src/app/api/channels/` 整目錄
- `src/app/api/workspaces/[id]/channels/` 整目錄
- `src/app/api/notify/` 整目錄（依賴 channel-notify、本 branch 砍）
- `src/components/workspace/` 整目錄（含 ChannelChat / ChannelSidebar / ChannelTabs / canvas / Share* dialogs / *Card / CanvasEditor、全 channel widget）

**Stores**：
- `src/stores/workspace/channels-store.ts`
- `src/stores/workspace/channel-store.ts`
- `src/stores/workspace/channel-group-store.ts`
- `src/stores/workspace/channel-member-store.ts`
- `src/stores/workspace/chat-store.ts`
- `src/stores/workspace/widgets-store.ts`
- `src/stores/workspace/message-store.ts`
- `src/stores/workspace/members-store.ts`（channel members）
- `src/stores/workspace/advance-list-store.ts`（channel widget、無人用）
- `src/stores/workspace/shared-order-list-store.ts`（同上）
- `src/stores/workspace/utils.ts`（純 message helpers）

**Lib / Service / Action / Entity**：
- `src/lib/notifications/` 整目錄
- `src/lib/channel-utils.ts`
- `src/lib/actions/channel-actions.ts`
- `src/data/entities/channel-members.ts`
- `src/features/tours/services/tour-channel.service.ts`
- `src/features/tours/components/TourChannelOperations.tsx`

### Src 修改（移除引用）

- `src/stores/workspace/index.ts`：重寫成只剩 workspace 本身、提供兼容 `useWorkspaceStore` / `useWorkspaceChannels` hook（轉接 createStore API、callers 不用全改）
- `src/stores/workspace/types.ts`：拿掉 `Channel` / `ChannelGroup` / `Message` / `MessageAttachment` / `RawMessage` / `Bulletin` / `PersonalCanvas` / `RichDocument` 等 type、保留 `Workspace` / `AdvanceList` / `SharedOrderList` / `AdvanceItem`
- `src/data/entities/index.ts`：拿掉 `channel-members` re-export
- `src/lib/actions/index.ts`：拿掉 `getOrCreateDmChannel` re-export
- `src/components/layout/sidebar.tsx`：拿掉 `/channel` menu item + `ChatDots` icon import
- `src/features/tours/hooks/useTourOperations.ts`：拿掉 `createTourChannel` 兩處調用（建團 + 轉開團都不再自動建頻道）
- `src/features/quotes/hooks/useQuoteTour.ts`：拿掉 `createChannelForTour` 調用
- `src/features/tours/components/ToursPage.tsx`：拿掉 `useTourChannelOperations` + `handleCreateChannel` props 傳遞
- `src/features/tours/components/TourActionButtons.tsx`：拿掉 `handleCreateChannel` prop（dead code）
- `src/features/tours/components/tour-overview.tsx`：拿掉 `useWorkspaceChannels` + `existingChannel` + `handleChannelClick` + `handleCreateChannel`（全 dead code）
- `src/app/(main)/orders/page.tsx`：`currentWorkspace` → `useAuthStore().user.workspace_id`
- `src/app/(main)/tours/[code]/page.tsx`：拿掉 channel button + `existingChannel`、`currentWorkspace?.id` → `user?.workspace_id`

### Docs（規範）

- `docs/VENTURO_ERP_STANDARDS.md` §16：「當前凍結：頻道聊天模組」改成「當前凍結：（無）」+ 加歷史紀錄「2026-05-02 同日凍結同日刪除」
- `docs/SCHEMA_PLAN.md`「內部頻道」section：從「🟡 凍結中」改成「🗑️ 已徹底刪除」、移除原本的解凍方向描述

## 驗證
- `tsc --noEmit` → 0 errors
- `check-standards.sh --strict` → 11/11 全綠
- DB query 確認 4 張表已不存在

## 結果
✅ 留下、ready for human review

- Branch: `auto/N-M04-delete-channel-system`
- 從 main 切、獨立 branch
- 改動：1 migration + 砍 ~80 個 src 檔 + 改 12 個 src 檔 + 改 2 個 docs

## 重大副作用 / 提醒

1. **既有用戶有開 24 個頻道、22 員工加入過** — DB CASCADE 全砍、資料無法復原
2. **未來重做聊天**需從 0 開始、不繼承現有半成品
3. **canvas 模組**（個人筆記本）一併被砍 — 從 grep 看也是 dead code、無人用
4. **AdvanceList / SharedOrderList type 保留**但 store 砍掉 — 之後用 type 的 dialog 也都被砍了、留 type 在 types.ts 是備援、可以下次清掉
5. **`useWorkspaceStore` / `useWorkspaceChannels` 兼容 hook 暫保留**：calendar / tenants 等 caller 還在用、之後逐步遷移到 `useWorkspaceStoreData` 直接用後可拿掉
