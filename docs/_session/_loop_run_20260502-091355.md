# Loop Run 2026-05-02 09:13:55

## Task
- ID: N-001（從 audit proposal 來、人類授權「do it」）
- 描述: admin client SSOT 收斂 — 散落 createClient + SERVICE_ROLE_KEY 全改用 `getSupabaseAdminClient()`、移除 `createServiceClient` 第二 SSOT

## 改動

### `src/lib/supabase/api-client.ts`
- 移除 `createServiceClient()` function（第二 SSOT、跟 `getSupabaseAdminClient` 重複）
- 移除 unused `createClient` import
- 註解修正：把指向 `createServiceClient` 的引導改成 `getSupabaseAdminClient`
- 保留 `createApiClient` (cookie-bound、RLS-aware) + `getCurrentWorkspaceId`

### `createServiceClient` 4 個 callers 改用 `getSupabaseAdminClient`
- `src/app/api/workspaces/[id]/route.ts`
- `src/app/api/workspaces/route.ts`
- `src/app/api/permissions/features/route.ts`
- `src/app/api/d/[code]/route.ts`

### 散落 `createClient(url, SERVICE_ROLE_KEY)` 改用 `getSupabaseAdminClient`（11 個檔成功改）
- `src/app/transport/[id]/confirm/page.tsx`
- `src/app/public/insurance/[code]/page.tsx`
- `src/app/api/customers/match/route.ts`
- `src/app/api/tours/by-code/[code]/route.ts`
- `src/app/api/transport/send-booking/route.ts`
- `src/app/api/contracts/sign/route.ts`
- `src/app/api/meta/webhook/route.ts`
- `src/app/api/itineraries/by-tour/[tourId]/route.ts`
- `src/app/api/line/send-insurance/route.ts`
- `src/app/api/employees/by-ref/[ref]/route.ts`
- `src/lib/ai-settings.ts`

### 保留未動的 8 個檔（解釋）

| 檔 | 為什麼保留 |
|---|---|
| `src/app/api/line/webhook/route.ts` | 用的是 raw `fetch(/rest/v1/...)` + apikey header、不是 createClient pattern。改成 admin client 要重寫 24 處 fetch 為 supabase query、屬「換實現方式」、超 N-001 範圍 |
| `src/app/api/line/webhook/[workspaceId]/route.ts` | 已是 `getSupabaseAdminClient` 用戶。剩下唯一 `SUPABASE_SERVICE_ROLE_KEY` reference 是當作 internal secret header（line 167）、不是 createClient |
| `src/app/api/accounting/vouchers/auto-create/route.ts` | typed admin client 暴露 `payment_request_items.amount` schema-code 不對齊 bug。改了會炸 type-check |
| `src/app/api/customers/link-line/route.ts` | typed admin client 暴露 customers insert payload type 對不上。同上 |
| `src/app/api/orders/create-from-booking/route.ts` | typed admin client 暴露 `member_type` required column 缺失（INSERT 漏給）。同上 |
| `src/app/public/contract/sign/[code]/page.tsx` | typed admin client 暴露 jsonb 欄位 property access + `member_ids` nullability 多處錯誤 |
| `src/lib/line/ai-customer-service.ts` | typed admin client 暴露 `tours.selling_prices` / `itineraries.name` / `suppliers.category` 三個欄位不存在 |
| `src/lib/notifications/channel-notify.ts` | typed admin client 暴露 `tours.channel_id` 不存在（2 處）|

**這些 schema-code 不對齊 bug 不是 N-001 造成的、是 N-001 暴露出來的**（原本散落的 `createClient(...)` 沒指定 `<Database>` generic、是 untyped client、所以 query 欄位不存在不會 type-check 報錯）。

## 驗證

- `tsc --noEmit` → 0 errors
- `check-standards.sh --strict` → 11/11 全綠
- 收斂前後比較：
  - 收斂前：散落 `process.env.SUPABASE_SERVICE_ROLE_KEY` 在 22 個檔 + 第二 SSOT `createServiceClient`
  - 收斂後：散落剩 8 個檔（2 個本質非 client SSOT 違反、6 個被 schema bug 卡住）+ 唯一 SSOT `getSupabaseAdminClient`

## 結果

✅ 留下、ready for human review

- Branch: `auto/N-001-consolidate-admin-clients`（從 main 切）
- 改 16 個 src/ 檔、無 migration、無 DB 變動、不 commit
- 跟先前 task（B-001~B-005、B-003 docs 改）的 working tree 改動共存

## 異常與發現

### 重大發現：6 個檔有 schema-code 不對齊 bug、之前被 untyped admin client 隱藏

完整 type error 清單（暴露 typed admin client 後）：

```
src/app/api/accounting/vouchers/auto-create/route.ts(282,26): payment_request_items.amount 不存在
src/app/api/accounting/vouchers/auto-create/route.ts(289,84): payment_request_items.amount 不存在
src/app/api/customers/link-line/route.ts(71,10): customers insert overload 不過
src/app/api/orders/create-from-booking/route.ts(42,8): member_type required column missing
src/app/api/orders/create-from-booking/route.ts(71,83): 同上 INSERT issue
src/app/public/contract/sign/[code]/page.tsx(39,59): contract_data.includeItinerary jsonb access
src/app/public/contract/sign/[code]/page.tsx(41,61): contract_data.includeMemberList jsonb access
src/app/public/contract/sign/[code]/page.tsx(45,5): contract.member_ids.length nullability
src/app/public/contract/sign/[code]/page.tsx(49,21): member_ids string[] | null
src/app/public/contract/sign/[code]/page.tsx(93,28): signer_type null assignment
src/lib/line/ai-customer-service.ts(115,3): tours.selling_prices 不存在
src/lib/line/ai-customer-service.ts(435,3): itineraries.name 不存在
src/lib/line/ai-customer-service.ts(475,3): suppliers.category 不存在
src/lib/notifications/channel-notify.ts(47,16): tours.channel_id 不存在
src/lib/notifications/channel-notify.ts(63,24): tours.channel_id 不存在
```

每個都需要業務語意決策：
- 是 code 用了 schema 沒有的欄位（dead code / 過時 query）→ 修 code
- 還是 schema 缺欄位（要 ALTER TABLE 加）→ 補 schema

不是 agent 能機械式決定的、屬 W 拍板層。

### 建議新 backlog task

| ID | 描述 | 風險 |
|---|---|---|
| **N-001b** | 6 個 schema-code 不對齊 bug 各自處理（每個檔一個小 task） | MEDIUM、需業務拍板每個欄位是補 schema 還是修 code |
| **N-001c** | `line/webhook/route.ts` 24 處 raw fetch 改用 admin client | MEDIUM、純機械但量大 |

### Branch / 檔案

- Branch: `auto/N-001-consolidate-admin-clients`
- 改 src 檔: 16 個
- 改 migration: 無
- 改 DB: 無
- 本 task 範圍以外的改動（從先前 branch 帶過來）：3 個 migration + 4 個 loop_run + 1 個 audit proposal + 2 個 docs（人類 review 時對照各 loop_run 報告分辨）
