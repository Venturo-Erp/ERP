# Loop Run 2026-05-02 09:44:48

## Task
- ID: N-001b（4 個 schema bug 修復、根據 William 2026-05-02 拍板）
- 描述: 一次處理 4 個 N-001 暴露的 schema-code 不對齊 bug

## William 拍板結果

| Bug | 拍板 | 我做 |
|---|---|---|
| 顧客連 LINE | 「先移開」 | 整檔砍 + middleware 引用清理 |
| 合約簽署 jsonb 讀法不對 | 「需要補上去」 | 修 page.tsx + 換 admin client |
| 會計傳票對應請款明細 | 「需要做更新」 | 把 `item.amount` 改成 `item.subtotal`（DB 真實欄位）+ 換 admin client |
| 頻道通知（tours.channel_id 不存在）| 「該功能已被徹底移除、移除所有 bug」 | 整套砍 |

## 改動

### 刪除（整套移除）
- `src/app/api/customers/link-line/route.ts`（顧客連 LINE 整檔）
- `src/app/api/notify/route.ts`（頻道通知 API）
- `src/lib/notifications/`（整目錄、含 channel-notify.ts + index.ts）

### 修改
- `src/middleware.ts`：拿掉 `/api/customers/link-line` 公開路徑條目
- `src/app/public/contract/sign/[code]/page.tsx`：
  - 把 `createClient` 換成 `getSupabaseAdminClient`
  - `contract.contract_data` jsonb 用 type narrow（typeof === 'object' && !isArray）後 cast 成 `Record<string, unknown>`
  - `member_ids` `?? []` 處理 null
  - `signer_type` / `signer_name` / `template` / `status` / `company_name` / `tours.location` / `tours.departure_date` / `tours.return_date` 補 null fallback、對齊 ContractSignPage 期待的型別
- `src/app/api/accounting/vouchers/auto-create/route.ts`：
  - 把 `createClient` + lazy init 換成 `getSupabaseAdminClient`
  - 借方分錄的 `item.amount` 改成 `item.subtotal`（line 293、300）

## DB 現況查核（沒動 schema、只查）
- `payment_request_items` 真實欄位：`subtotal` (numeric)、`unitprice` (numeric)、**沒有 amount**
- `expense-core.service.ts` 註解確認「加總 `payment_request_items.subtotal` 為 total_cost」 — 跟 `subtotal` 一致

## 驗證
- `tsc --noEmit` → 0 errors
- `check-standards.sh --strict` → 11/11 全綠

## 結果
✅ 留下、ready for human review

- Branch: `auto/N-001b-fix-schema-bugs`
- 從 main 切、獨立 branch、純 src + 1 個 middleware 改

## N-001 暴露的 6 個 bug 處理進度

| Bug | 狀態 |
|---|---|
| ✅ tours.channel_id (channel-notify.ts) | 整套砍 |
| ⏳ tours.selling_prices / itineraries.name / suppliers.category (ai-customer-service.ts) | 下一個 branch（LINE 客服 AI 撤） |
| ✅ payment_request_items.amount (vouchers/auto-create) | 改 subtotal |
| ⏳ member_type missing (orders/create-from-booking) | 等 William 從 4 選項拍板（見 _investigation_member_type_20260502.md） |
| ✅ customers/link-line type | 整檔砍 |
| ✅ contract_data jsonb (public/contract/sign) | 修對 |

剩 2 個（LINE 客服 AI + member_type 拍板）。
